public abstract class Animal {
    //this class can only created when a child class inherits it

    //an abstract method cannot have a body
    //body must be implemented when child class is created
    abstract void makeNoise();

}
